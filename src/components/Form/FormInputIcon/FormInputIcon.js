import styled from '@emotion/styled';
import { Button } from '@mui/material';

export const FormInputIcon = styled(Button)`
	position: absolute;
	top: 50%;
	transform: translateY(-50%);
	right: 10px;
	width: 50px;
`